#include<stdio.h>
int even(int int);
int main()
{
	int start,end;
scanf ("%d %d",&start,&end);
printf("%d",even(start,end));
}
int even(int start, int end) 
{
    if(start > end)
        return 0;
    else
        return (start + even(start + 2, end));
}
